# Magic Metal — phase log — 2026-09-09

## Resumed stage

The previously interrupted Tube Master work is resumed on branch `feat/tube-master-mvp`.

## Completed in this stage

- Reconfirmed the project memory and existing architecture before changing code.
- Preserved the single public catalog model and hidden internal supplier layers.
- Preserved the distinction `CAPABILITY != OFFER != AVAILABILITY`.
- Kept E-Metall as a priority commercial offer source; 23met and Metal100 remain discovery/market-signal layers.
- Kept Metallservice as reference assortment + one price source, not a market-price floor.
- Added initial regional factory capability seeds, including Noginsk and Tambov, alongside Ural and specialist producers.
- Added an internal manager-only tube search endpoint at `/api/internal/tubes/search`.
- The endpoint accepts `size`, `diameter`, `wall`, `grade`, `standard`, and `type` parameters and returns factory capabilities plus ranked offers.
- Production access to the internal endpoint is intentionally gated by `TUBE_SEARCH_API_TOKEN`; it is not a public supplier API.
- Existing Tube Master unit tests cover exact capability, range exclusion, and verified-stock ranking behavior.

## Next stage

1. Replace range-only factory seeds with full discrete D×S tables from official factory sources.
2. Normalize E-Metall records into `diameterMm` / `wallMm` / `grade` / `standard` / location / quantity / freshness.
3. Add adapters for E-Metall, 23met, Metal100, Metallservice and selected factory catalogues.
4. Add a manager UI that consumes the internal endpoint without exposing supplier identity to customers.
5. Add commercial scoring: price + logistics + lead time + minimum lot + documents + freshness + risk.
6. Run the project’s profile tests, then full lint/build before merging or publishing.

## Safety / data rules

- Never commit passwords, API keys, 2FA codes or supplier private snapshots.
- Never infer stock from production capability.
- Never expose supplier layers in the public catalog.
- Never publish an unverified price, stock level or delivery term as a confirmed fact.
